# 40. 统一音乐媒体解析链运行态验证

## 结论

`MusicMediaInput -> MusicMetaBase -> MusicMediaInfo` 已经成为当前版本的统一音乐媒体解析主路径，并已接管 discovery、detail、search job、subscription execution、organize 上游输入与 chart entry 订阅快照。

## 已验证内容

- backend 全量测试通过
- frontend 构建与单测通过
- plugin runtime 打包通过
- `/media/resolve/detail` 返回统一链解析结果
- 旧的元数据桥接接口保持移除状态
- RSS / 弱来源 chart entry 会持久化统一链显式字段
- 订阅执行与 organize 上游会优先消费统一链结果

## 当前状态

- discovery 下钻已切到统一媒体解析链
- chart 列表与详情已持久化
- SearchJob、Subscription、SubscriptionRun、OrganizeRecord 已显式保存统一链对象
- 后续工作主要集中在宿主验证、下载完成闭环与运行态诊断
